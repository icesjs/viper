# 默认的环境变量配置（构建时会默认加载）
# 调试日志设置
#DEBUG=*
#DEBUG=builder-scripts:*
#DEBUG=builder-scripts:compile
#DEBUG=builder-scripts:app
#DEBUG=viper:*
#DEBUG=viper:dev
#DEBUG=viper:prod

# 启用electron模块的代理（仅在开发模式时有效）
# 启用代理后，会对一些模块功能进行拦截处理，以便更好的适用开发
# 比如初次启动应用时，打开应用窗口不会自动聚焦，就是通过代理拦截方法来实现的
USE_MODULE_PROXY_FOR_ELECTRON=true

# 自定义HTTP开发服务器监听地址
#HOST=localhost

# 自定义HTTP服务端口号
PORT=3000

# 进行产品构建时生成映射文件
GENERATE_SOURCEMAP=false

# 用于构建内部通信的websoket端口号
DETACHED_WEB_SOCKET_PORT=9527

# windows平台下，spwan子进程执行命令行程序会导致出现命令提示符窗口
# 所以默认在windows下，将electron命令提示符终端以后台模式运行，以规避弹窗
# 开启此项环境变量，可以将electron命令提示符终端以前台模式运行
NOT_DETACH_ELECTRON_ON_WINDOWS=false

# 自动打开开发者工具
AUTO_OPEN_DEV_TOOLS=true

# 代码变更时自动重启应用
AUTO_RELAUNCH_APP=true

# 重启延时(ms)
AUTO_RELAUNCH_DELAY=5000

# 开发模式下是否显示不自动聚焦的窗口
WINDOW_FIRST_SHOW_INACTIVE=true

# 设置应用开发日志的输出级别(产品模式下不会输出开发日志)
APP_DEV_LOG_LEVEL=info

# 设置应用产品日志的输出级别(仅在开发模式下生效)
APP_PRO_LOG_LEVEL=info

# 是否将调试日志写到日志文件中
WRITE_LOGS_TO_FILE=false

# 定义渲染进程的构建目标环境(有效值：web或electron-renderer)
RENDERER_BUILD_TARGET=electron-renderer